services:
    db:
        image: 'postgres:13'
        ports:
            - '5433:5432'
        environment:
            - POSTGRES_USER=local
            - POSTGRES_PASSWORD=local
            - POSTGRES_DB=assetflow
        volumes:
            - postgres_data:/var/lib/postgresql/data

volumes:
    postgres_data: